export default class Ball {
    constructor(ballElem) {
        this.ballElem = ballElem
    }

    // helper functions

    get x() {

    }

    update(delta) {
        // what are we updating? x and y position, velocity, and direction

    }
}